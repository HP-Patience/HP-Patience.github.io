# Celyn_blog 博客项目使用说明

## 项目概述

| 项目 | 信息 |
|------|------|
| **框架** | Hexo 7.3.0 |
| **主题** | Icarus 6.x |
| **部署平台** | GitHub Pages |
| **网站地址** | https://hp-patience.github.io |
| **本地预览** | http://localhost:4002 |

---

## 目录结构

```
blog/
├── _config.yml              # Hexo 主配置文件
├── _config.icarus.yml        # Icarus 主题配置
├── package.json              # 依赖管理
├── scaffolds/                # 文章模板
│   ├── post.md             # 文章模板
│   ├── page.md             # 页面模板
│   └── draft.md            # 草稿模板
├── source/                  # 源文件目录
│   ├── _posts/             # 文章存放目录（重要！）
│   ├── about/              # 关于页面
│   ├── categories/         # 分类页面
│   ├── tags/               # 标签页面
│   ├── link/               # 友链页面
│   ├── movies/             # 电影页面
│   └── music/              # 音乐页面
├── scripts/                 # 自定义脚本
│   └── story-generator.js  # Story 页面生成器
├── themes/
│   └── icarus/             # Icarus 主题目录
└── STORY使用说明.md          # Story 功能说明
```

---

## 快速开始

### 环境要求

- Node.js >= 14
- Git
- npm 或 yarn

### 安装依赖

```bash
cd blog
npm install
```

### 本地预览

```bash
# 启动开发服务器（默认端口 4000）
hexo server

# 或指定端口
hexo server -p 4002
```

访问 http://localhost:4002 查看博客

### 常用命令

```bash
# 创建新文章
hexo new "文章标题"

# 创建新页面
hexo new page "页面名称"

# 清除缓存和生成的文件
hexo clean

# 生成静态文件
hexo generate

# 部署到 GitHub
hexo deploy

# 一键生成并部署
hexo clean && hexo generate && hexo deploy
```

---

## 写作指南

### 文章 Front-matter 模板

```yaml
---
title: 文章标题
date: 2026-05-01 18:00:00
categories:
  - 分类名称    # 可选，支持多个分类
tags:
  - 标签1        # 可选，支持多个标签
  - 标签2
cover: /img/封面图.jpg  # 可选，文章封面图
---

文章正文内容...
```

### Markdown 扩展语法

本项目支持以下 Markdown 扩展：

#### 1. 容器块（提示框）

```markdown
::: success
成功提示内容
:::

::: tips
提示内容
:::

::: warning
警告内容
:::

::: danger
危险/错误提示
:::
```

#### 2. 数学公式（MathJax）

```markdown
行内公式：$E = mc^2$

独立公式：
$$
\int_0^\infty e^{-x^2} dx = \frac{\sqrt{\pi}}{2}
$$
```

#### 3. 复选框

```markdown
- [x] 已完成事项
- [ ] 未完成事项
```

#### 4. 上标/下标

```markdown
上标：H~2~O
下标：X^2^
```

#### 5. 缩写/脚注

```markdown
缩写：*[HTML]: Hyper Text Markup Language

脚注：[^1]
[^1]: 脚注内容
```

#### 6. 图片尺寸调整

```markdown
![图片](url){width=500px}
![图片](url){height=300px}
```

#### 7. 折叠内容

```markdown
<details>
<summary>点击展开</summary>

折叠的内容...

</details>
```

---

## 页面功能说明

### 导航栏菜单

| 菜单项 | 路径 | 说明 |
|--------|------|------|
| Home | `/` | 首页，显示最新文章列表 |
| Archives | `/archives` | 归档，按时间查看所有文章 |
| Categories | `/categories` | 分类，按分类浏览文章 |
| Tags | `/tags` | 标签，按标签浏览文章 |
| **Story** | `/story` | **生活故事**，记录日常生活（不在首页显示） |
| About | `/about` | 关于页面 |

### Story 页面（特殊功能）

Story 页面用于发布生活类文章，这些文章**不会出现在首页**。

**创建 Story 文章：**

```yaml
---
title: 我的生活故事
date: 2026-05-01 18:00:00
categories:
  - Story        # 关键！添加此分类
tags:
  - 生活
---

故事内容...
```

> 详细说明请参阅 [STORY使用说明.md](./STORY使用说明.md)

---

## 配置说明

### 主配置 (_config.yml)

#### 站点基本信息

```yaml
title: Celyn_blog           # 网站标题
subtitle: ''                 # 副标题
description: 费曼学习法...   # 网站描述
keywords: 学习记录          # 关键词
author: Celyn               # 作者
language: zh-CN             # 语言
timezone: Asia/Shanghai    # 时区
```

#### URL 设置

```yaml
url: https://hp-patience.github.io
permalink: :year/:month/:day/:title/
```

#### 首页设置

```yaml
index_generator:
  path: ''
  per_page: 10               # 每页显示文章数
  order_by: -date           # 按时间倒序

hide_categories:             # 在首页隐藏的分类
  - Story
```

#### 部署设置

```yaml
deploy:
  type: git
  repository: git@github.com:HP-Patience/HP-Patience.github.io.git
  branch: main
```

### 主题配置 (_config.icarus.yml)

#### 导航栏

```yaml
navbar:
  menu:
      Home: /
      Archives: /archives
      Categories: /categories
      Tags: /tags
      Story: /story
      About: /about
  links:
      GitHub:
          icon: fab fa-github
          url: https://github.com/HP-Patience
```

#### 侧边栏组件

当前配置的组件：

| 位置 | 组件 | 说明 |
|------|------|------|
| 左侧 | 个人资料 | 作者头像、简介、社交链接 |
| 左侧 | 友情链接 | 外部链接 |
| 左侧 | 分类列表 | 显示所有分类 |
| 右侧 | 最新文章 | 最近5篇文章 |
| 右侧 | 归档 | 月度归档 |
| 右侧 | 标签云 | 随机显示20个标签 |

#### 文章设置

```yaml
article:
  highlight:
      theme: atom-one-light   # 代码高亮主题
      clipboard: true         # 复制代码按钮
      fold: unfolded          # 代码折叠状态
  readtime: true              # 显示阅读时间
  update_time: true           # 显示更新时间
```

#### 评论系统

使用 Utterances（基于 GitHub Issues）：

```yaml
comment:
  type: utterances
  repo: HP-Patience/HP-Patience.github.io
  issue_term: title
  theme: github-light
```

#### 搜索功能

使用 Insight 搜索：

```yaml
search:
  type: insight
  insight:
      cdn: true
```

---

## 自定义功能

### 1. Story 页面生成器

位置：`scripts/story-generator.js`

作用：筛选 `Story` 分类的文章，生成独立的 Story 列表页。

### 2. 自定义样式

自定义 CSS 文件位于 `source/css/`：

- `admin-style.css` - 后台管理样式
- `butterfly_article_double_row.css` - 双栏文章布局
- `categories_dark_fit.css` - 分类页暗色适配

### 3. 自定义脚本

位于 `source/js/`：

- `footer_runtime_Fomalhaut.js` - 页脚运行时间计时器

---

## 发布流程

### 方式一：命令行部署

```bash
# 1. 清理缓存
hexo clean

# 2. 生成静态文件
hexo generate

# 3. 部署到 GitHub
hexo deploy
```

### 方式二：Hexo Admin 后台管理

访问 http://localhost:4002/admin/ 使用后台管理界面：

- 用户名：admin
- 密码：（已配置）

在后台可以：
- 创建/编辑/删除文章
- 管理页面
- 预览效果

### 推荐工作流

```bash
# 1. 创建文章
hexo new "新文章标题"

# 2. 编辑文章
# 打开 source/_posts/新文章标题.md 进行编辑

# 3. 本地预览
hexo server -p 4002
# 访问 http://localhost:4002 检查效果

# 4. 确认无误后部署
hexo clean && hexo generate && hexo deploy
```

---

## 常见问题

### Q: 修改配置后没有生效？

**A**: 运行 `hexo clean && hexo generate` 清除缓存后重新生成。

### Q: 图片无法显示？

**A**: 
1. 确保图片放在与文章同名的文件夹中（需开启 `post_asset_folder: true`）
2. 或将图片放在 `source/img/` 目录下
3. 引用方式：`![描述](图片名.png)` 或 `![描述](/img/图片名.png)`

### Q: 如何修改侧边栏？

**A**: 编辑 `_config.icarus.yml` 中的 `widgets` 部分，可添加/移除/排序组件。

### Q: 如何更换代码高亮主题？

**A**: 在 `_config.icarus.yml` 中修改：
```yaml
article:
  highlight:
      theme: atom-one-dark  # 更换为其他主题
```

可选主题：`atom-one-light`, `atom-one-dark`, `github`, `monokai`, `vs` 等

### Q: 如何添加新的导航菜单项？

**A**: 
1. 在 `_config.icarus.yml` 的 `navbar.menu` 中添加
2. 在 `source/` 下创建对应页面

### Q: Story 文章还是显示在首页？

**A**: 检查：
1. `_config.yml` 是否有顶层 `hide_categories: - Story`
2. 文章的 `categories` 是否包含 `Story`
3. 运行 `hexo clean` 清除缓存

### Q: 如何备份博客源码？

**A**: 建议使用 Git 管理，主要备份：
- `_config.yml`
- `_config.icarus.yml`
- `source/` 目录（尤其是 `_posts/`）
- `themes/icarus/` （如有自定义修改）
- `scripts/` 目录

---

## 技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| Hexo | 7.3.0 | 静态站点生成器 |
| Icarus | 6.x | 博客主题 |
| Inferno | 3.x | React-like 渲染引擎 |
| markdown-it | 7.x | Markdown 解析器 |
| MathJax | - | 数学公式渲染 |
| highlight.js | - | 代码高亮 |
| Utterances | - | 评论系统 |
| hexo-generator-indexed | 1.x | 首页生成器（支持隐藏分类） |
| hexo-admin | 2.x | 后台管理插件 |

---

## 相关链接

- [Hexo 官方文档](https://hexo.io/docs/)
- [Icarus 主题文档](https://ppoffice.github.io/hexo-theme-icarus/)
- [Markdown 语法参考](https://markdown.com.cn/basic-syntax/)
- [MathJax 公式手册](https://mathjax.org/)
- [Font Awesome 图标](https://fontawesome.com/icons)

---

## 更新日志

### 2026-05-01
- 新增 Story 页面功能
- 配置 hexo-generator-indexed 插件
- 添加 Story 页面生成器脚本
